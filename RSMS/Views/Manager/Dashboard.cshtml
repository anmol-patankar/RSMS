@{
    List<UserInfo> allEmployees = ViewBag.Employees;
}
<h1>Welcome to the Manager Dashboard</h1>
<div>

    <p>This is your manager dashboard. You can manage employees, products, and transactions here.</p>

    <button id="showEmployeesBtn" class="toggle-btn">Show Employees</button>
    <button id="showProductsBtn" class="toggle-btn">Show Products</button>
    <button id="showTransactionsBtn" class="toggle-btn">Show Transactions</button>
    <div style="margin-top: 20px;">
        <table id="employeeTable" class="data-table">
            <caption class="table-caption">List of employees in your store</caption>
            <thead>
                <tr>
                    <th>Employee ID</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in allEmployees)
                {
                    <tr>
                        <td>@employee.UserId</td>
                        <td>@employee.FirstName</td>
                        <td>@employee.LastName</td>
                        <td>@employee.Email</td>
                        <td>@employee.Phone</td>
                    </tr>
                }
            </tbody>
        </table>
        @*
        <table id="productTable" class="data-table" style="display: none;">
        <caption class="table-caption">List of products in your store</caption>
        <thead>
        <tr>
        <th>Product ID</th>
        <th>Product Code</th>
        <th>Name</th>
        <th>Description</th>
        <th>Price (Before Tax)</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var product in allProducts)
        {
        <tr>
        <td>@product.ProductId</td>
        <td>@product.ProductCode</td>
        <td>@product.Name</td>
        <td>@product.Description</td>
        <td>@product.PriceBeforeTax</td>
        </tr>
        }
        </tbody>
        </table>

        <table id="transactionTable" class="data-table" style="display: none;">
        <caption class="table-caption">List of transactions in your store</caption>
        <thead>
        <tr>
        <th>Transaction ID</th>
        <th>Employee ID</th>
        <th>Product ID</th>
        <th>Quantity</th>
        <th>Transaction Date</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var transaction in allTransactions)
        {
        <tr>
        <td>@transaction.TransactionId</td>
        <td>@transaction.EmployeeId</td>
        <td>@transaction.ProductId</td>
        <td>@transaction.Quantity</td>
        <td>@transaction.TransactionDate</td>
        </tr>
        }
        </tbody>
        </table> *@
    </div>
</div>